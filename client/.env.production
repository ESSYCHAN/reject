VITE_CLERK_PUBLISHABLE_KEY=pk_test_Y2l2aWwtc3dpbmUtMTUuY2xlcmsuYWNjb3VudHMuZGV2JA
VITE_API_URL=
# Agents (Python FastAPI) service. The previous value (talented-rebirth-*) was
# STALE — it now serves an unrelated app. The live agents service is below.
# Note: the production build may instead use the GitHub Actions `VITE_AGENTS_API_URL`
# repo variable (see .github/workflows/deploy.yml) — keep both in sync.
VITE_AGENTS_API_URL=https://agents-production-3306.up.railway.app
